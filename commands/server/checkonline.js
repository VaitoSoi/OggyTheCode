const { Message } = require('discord.js')

module.exports = {
    name: 'checkonline',
    aliases: ['conl'],
    description: 'Kiểm tra xem player có online trong server anarchyvn hay không',
    usage: '<Tên player, vd: VaitoSoi>',
    /**
     * @param {Message} message 
     */
    run: async (client, message, args) => {
        return
    }
}
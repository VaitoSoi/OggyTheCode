/*
const mineflayer = require('mineflayer')
const viewer = require('prismarine-viewer')

module.exports = {
    name: 'spawn',
    /**
     * 
     * @param {mineflayer.Bot} bot 
     *
    async run(bot) {
        if (bot.view == false) {
            try {
                viewer.mineflayer(bot, {
                    port: process.env.MC_VIEWER
                })
                console.log(`[MINECRAFT] Running at https://localhost:${process.env.MC_VIEWER}`)
                bot.view = true
            } catch (error) {

            }
        }
    }
}
*/
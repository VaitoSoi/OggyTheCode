const ms = require('ms')
const arrayOfStatus = require('../info/statusArray')
const { Client, Collection } = require('discord.js')

module.exports = {
    name: 'ready',
    /**
     * 
     * @param {Client} client 
     */
    run(client) {
        
    }
}
const { Message } = require('discord.js')

module.exports = {
    name: 'players-online',
    aliases: ['ponl', 'playeronline', 'player-online'],
    description: 'Kiểm tra các player hiện đang online trong server bot đang ở',
    usage: '<Tên player, vd: VaitoSoi>',
    /**
     * @param {Message} message 
     */
    run: async (client, message, args) => {
        return
    }
}